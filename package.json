{
  "name": "axilattice",
  "version": "8.0.0",
  "private": true,
  "description": "In-browser analytics engine that reports the statistical test behind every claim.",
  "engines": {
    "node": ">=18 <=20"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "recharts": "2.13.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "CI=false react-scripts build",
    "test": "node tests/run.mjs",
    "test:engine": "node tests/run.mjs"
  },
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  },
  "eslintConfig": { "extends": ["react-app"] }
}
